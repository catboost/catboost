RESOURCES_LIBRARY()



SET(NEED_PLATFORM_PEERDIRS no)

IF (ARCH_ARM7 OR ARCH_ARM64)
    # iOS 13.1 SDK / Xcode 11.1 (11A1027)
    DECLARE_EXTERNAL_RESOURCE(IOS_SDK_ROOT sbr:1184199239)
ELSEIF (ARCH_I386 OR ARCH_X86_64)
    # iOS Simulator 13.1 SDK / Xcode 11.1 (11A1027)
    DECLARE_EXTERNAL_RESOURCE(IOS_SDK_ROOT sbr:1184198391)
ELSE()
    MESSAGE(FATAL_ERROR "There is no iOS SDK for the selected target platform")
ENDIF()

END()
