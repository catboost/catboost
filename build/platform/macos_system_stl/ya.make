RESOURCES_LIBRARY()



# Taken from the default toolchain of the Xcode 12.5.1
DECLARE_EXTERNAL_RESOURCE(MACOS_SYSTEM_STL sbr:2490861692)

IF (USE_STL_SYSTEM)
    CFLAGS(
        GLOBAL -I${MACOS_SYSTEM_STL_RESOURCE_GLOBAL}
    )
ENDIF()

END()
