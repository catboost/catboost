RESOURCES_LIBRARY()



IF (USE_SYSTEM_JSTYLE_LIB)
    MESSAGE(WARNING System java codestyle library $USE_SYSTEM_JSTYLE_LIB will be used)
ELSE()
    IF (JDK_REAL_VERSION < 11)
        DECLARE_EXTERNAL_RESOURCE(JSTYLE_LIB sbr:3122361336)
    ELSE()
        DECLARE_EXTERNAL_RESOURCE(JSTYLE_LIB sbr:3122148726)
    ENDIF()
ENDIF()

END()
