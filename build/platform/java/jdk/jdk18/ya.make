
RESOURCES_LIBRARY()
INCLUDE(${ARCADIA_ROOT}/build/platform/java/jdk/resources.inc)

DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
    JDK18
    ${JDK18_DARWIN} FOR DARWIN
    ${JDK18_DARWIN_ARM64} FOR DARWIN-ARM64
    ${JDK18_LINUX} FOR LINUX
    ${JDK18_WINDOWS} FOR WIN32
)

IF(OS_DARWIN AND ARCH_ARM64)
    DECLARE_EXTERNAL_RESOURCE(WITH_JDK18 ${JDK18_DARWIN_ARM64})
ELSEIF(OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(WITH_JDK18 ${JDK18_DARWIN})
ELSEIF(OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(WITH_JDK18 ${JDK18_LINUX})
ELSEIF(OS_WINDOWS)
    DECLARE_EXTERNAL_RESOURCE(WITH_JDK18 ${JDK18_WINDOWS})
ENDIF()

END()
