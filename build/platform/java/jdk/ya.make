RESOURCES_LIBRARY()


IF(USE_SYSTEM_JDK)
    MESSAGE(WARNING System JDK $USE_SYSTEM_JDK will be used)
ELSEIF(JDK_VERSION STREQUAL "12")
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        JDK
        sbr:1024709691 FOR DARWIN
        sbr:1024708480 FOR LINUX
        sbr:1024706391 FOR WIN32
    )
    IF(NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS AND NOT HOST_OS_DARWIN)
        MESSAGE(FATAL_ERROR Unsupported platform for JDK12)
    ENDIF()
ELSEIF(JDK_VERSION STREQUAL "11")
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        JDK
        sbr:1024418107 FOR DARWIN
        sbr:1024427521 FOR LINUX
        sbr:1024431126 FOR WIN32
    )
    IF(NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS AND NOT HOST_OS_DARWIN)
        MESSAGE(FATAL_ERROR Unsupported platform for JDK11)
    ENDIF()
    IF(SANITIZER_TYPE STREQUAL "address")
        IF(HOST_OS_LINUX)
            DECLARE_EXTERNAL_RESOURCE(JDK_FOR_TESTS sbr:893693905)
        ELSE()
            MESSAGE(FATAL_ERROR Unsupported platform for JDK11 with asan)
        ENDIF()
    ENDIF()
ELSEIF(JDK_VERSION STREQUAL "11Shenandoah")
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        JDK
        sbr:898173174 FOR LINUX
    )
    IF(NOT HOST_OS_LINUX)
        MESSAGE(FATAL_ERROR Unsupported platform for JDK11 with shenandoah gc)
    ENDIF()
ELSEIF(JDK_VERSION STREQUAL "10")
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        JDK
        sbr:545649998 FOR DARWIN
        sbr:545649806 FOR LINUX
        sbr:545648079 FOR WIN32
    )
    IF(NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS AND NOT HOST_OS_DARWIN)
        MESSAGE(FATAL_ERROR Unsupported platform for JDK10)
    ENDIF()
ELSEIF(JDK_VERSION STREQUAL "8")
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        JDK
        sbr:810369364 FOR DARWIN
        sbr:810373574 FOR LINUX
        sbr:810375867 FOR WIN32
    )
    IF(NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS AND NOT HOST_OS_DARWIN)
        MESSAGE(FATAL_ERROR Unsupported platform for JDK8)
    ENDIF()
ELSE()
    MESSAGE(FATAL_ERROR Unsupported JDK version)
ENDIF()

END()
