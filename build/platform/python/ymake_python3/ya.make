RESOURCES_LIBRARY()



IF (
       OS_DARWIN AND ARCH_ARM64
    OR OS_DARWIN AND ARCH_X86_64
    OR OS_LINUX AND ARCH_ARRCH64
    OR OS_LINUX AND ARCH_PPC64LE
    OR OS_LINUX AND ARCH_X86_64
    OR OS_WINDOWS AND ARCH_X86_64
    )
    DECLARE_EXTERNAL_RESOURCE_BY_JSON(YMAKE_PYTHON3 resources.json)
ELSE()
    DECLARE_EXTERNAL_RESOURCE(YMAKE_PYTHON3 sbr:3877390008)
ENDIF()

END()

RECURSE(
    bin
)
