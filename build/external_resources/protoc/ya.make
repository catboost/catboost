RESOURCES_LIBRARY()

IF(HOST_OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(EXTERNAL_PROTOC sbr:1114867177)
ELSEIF(HOST_OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(EXTERNAL_PROTOC sbr:1114860371)
ELSE()
    MESSAGE(FATAL_ERROR Unsupported host platfrom for protoc toolchain)
ENDIF()
CFLAGS(GLOBAL -isystem GLOBAL $EXTERNAL_PROTOC_RESOURCE_GLOBAL/include)
END()
