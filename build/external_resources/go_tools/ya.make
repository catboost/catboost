

RESOURCES_LIBRARY()

IF (HOST_OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:743449103)
ELSEIF (HOST_OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:743451200)
ELSEIF (HOST_OS_WINDOWS)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:743452740)
ELSE()
    MESSAGE(FATAL_ERROR Unsupported host platform)
ENDIF()

END()
