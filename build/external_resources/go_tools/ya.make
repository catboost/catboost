

RESOURCES_LIBRARY()

IF(GOSTD_VERSION STREQUAL 1.13.6)
    IF (HOST_OS_LINUX)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1298915066)
    ELSEIF (HOST_OS_DARWIN)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1298916115)
    ELSEIF (HOST_OS_WINDOWS)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1298917088)
    ELSE()
        MESSAGE(FATAL_ERROR Unsupported host platform)
    ENDIF()
ELSEIF(GOSTD_VERSION STREQUAL 1.14.1)
    IF (HOST_OS_LINUX)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1410185294)
    ELSEIF (HOST_OS_DARWIN)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1410187392)
    ELSEIF (HOST_OS_WINDOWS)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1410189575)
    ELSE()
        MESSAGE(FATAL_ERROR Unsupported host platform)
    ENDIF()
ELSE()
    MESSAGE(FATAL_ERROR Unsupported version [$GOSTD] of Go Standard Library)
ENDIF()

END()
