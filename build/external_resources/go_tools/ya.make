

RESOURCES_LIBRARY()

IF (NOT HOST_OS_DARWIN AND NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS)
    MESSAGE(FATAL_ERROR Unsupported host platform for GO_TOOLS)
ELSEIF(GOSTD_VERSION == 1.16.3)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:2088833999 FOR DARWIN
        sbr:2088830421 FOR LINUX
        sbr:2088838077 FOR WIN32
    )
ELSEIF(GOSTD_VERSION == 1.16.4)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:2165716625 FOR DARWIN-ARM64
        sbr:2152566699 FOR DARWIN
        sbr:2152565165 FOR LINUX
        sbr:2152568492 FOR WIN32
    )
ELSE()
    MESSAGE(FATAL_ERROR Unsupported version [${GOSTD_VERSION}] of Go Standard Library)
ENDIF()

END()
