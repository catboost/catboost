

RESOURCES_LIBRARY()

IF (NOT HOST_OS_DARWIN AND NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS)
    MESSAGE(FATAL_ERROR Unsupported host platform for GO_TOOLS)
ELSEIF(GOSTD_VERSION STREQUAL 1.15.4)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:1816827315 FOR DARWIN
        sbr:1816826515 FOR LINUX
        sbr:1816828384 FOR WIN32
    )
ELSEIF(GOSTD_VERSION STREQUAL 1.15.6)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:1866035681 FOR DARWIN
        sbr:1866033127 FOR LINUX
        sbr:1866039068 FOR WIN32
    )
ELSE()
    MESSAGE(FATAL_ERROR Unsupported version [${GOSTD_VERSION}] of Go Standard Library)
ENDIF()

END()
