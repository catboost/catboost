

RESOURCES_LIBRARY()

IF(GOSTD_VERSION STREQUAL 1.12.9)
    IF (HOST_OS_LINUX)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1078836585)
    ELSEIF (HOST_OS_DARWIN)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1078837434)
    ELSEIF (HOST_OS_WINDOWS)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1078838261)
    ELSE()
        MESSAGE(FATAL_ERROR Unsupported host platform)
    ENDIF()
ELSEIF(GOSTD_VERSION STREQUAL 1.13)
    IF (HOST_OS_LINUX)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1106407325)
    ELSEIF (HOST_OS_DARWIN)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1106408428)
    ELSEIF (HOST_OS_WINDOWS)
        DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:1106409385)
    ELSE()
        MESSAGE(FATAL_ERROR Unsupported host platform)
    ENDIF()
ELSE()
    MESSAGE(FATAL_ERROR Unsupported version [$GOSTD] of Go Standard Library)
ENDIF()

END()
