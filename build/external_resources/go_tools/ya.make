

RESOURCES_LIBRARY()

IF (NOT HOST_OS_DARWIN AND NOT HOST_OS_LINUX AND NOT HOST_OS_WINDOWS)
    MESSAGE(FATAL_ERROR Unsupported host platform for GO_TOOLS)
ELSEIF(GOSTD_VERSION STREQUAL 1.14.3)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:1506989651 FOR DARWIN
        sbr:1506987739 FOR LINUX
        sbr:1506991448 FOR WIN32
    )
ELSEIF(GOSTD_VERSION STREQUAL 1.14.4)
    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        GO_TOOLS
        sbr:1539497911 FOR DARWIN
        sbr:1539496897 FOR LINUX
        sbr:1539499264 FOR WIN32
    )
ELSE()
    MESSAGE(FATAL_ERROR Unsupported version [${GOSTD_VERSION}] of Go Standard Library)
ENDIF()

END()
