

RESOURCES_LIBRARY()

IF (OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:701958445)
ELSEIF (OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:701961386)
ELSEIF (OS_WINDOWS)
    DECLARE_EXTERNAL_RESOURCE(GO_TOOLS sbr:701967456)
ELSE()
    MESSAGE(FATAL_ERROR Unsupported host platform)
ENDIF()

END()
