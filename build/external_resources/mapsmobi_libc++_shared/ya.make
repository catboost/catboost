RESOURCES_LIBRARY()



IF (OS_ANDROID)
    IF (ARCH_ARM7)
        DECLARE_EXTERNAL_RESOURCE(MAPSMOBI_LIBCPP sbr:1098009672)
    ELSEIF (ARCH_ARM64)
        DECLARE_EXTERNAL_RESOURCE(MAPSMOBI_LIBCPP sbr:1098012508)
    ELSEIF (ARCH_I386)
        DECLARE_EXTERNAL_RESOURCE(MAPSMOBI_LIBCPP sbr:1098014695)
    ELSEIF (ARCH_X86_64)
        DECLARE_EXTERNAL_RESOURCE(MAPSMOBI_LIBCPP sbr:1098016456)
    ELSE()
        MESSAGE(FATAL_ERROR Unsupported platform)
    ENDIF()
ELSE()
    MESSAGE(FATAL_ERROR Unsupported platform)
ENDIF()

END()
