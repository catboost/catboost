{
    "name": "typescript",
    "version": "4.6.2",
    "description": "typescript compiler bundle",
    "comment": "Warning: обратите внимание, что меняя версию typescript необходимо поменять её и в ../webpack ресурсе",
    "dependencies": {
        "ts-patch": "2.0.2",
        "typescript": "4.6.2"
    },
    "scripts": {
        "prepare": "ts-patch v && ts-patch i -d node_modules && npm uninstall --no-save ts-patch"
    }
}
