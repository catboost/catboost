

RESOURCES_LIBRARY()

IF (HOST_OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(DELVE sbr:1442930320)
ELSEIF (HOST_OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(DELVE sbr:1442929802)
ELSEIF (HOST_OS_WINDOWS)
    DECLARE_EXTERNAL_RESOURCE(DELVE sbr:1442930011)
ELSE()
    MESSAGE(FATAL_ERROR Unsupported host platform)
ENDIF()

END()
