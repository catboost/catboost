

RESOURCES_LIBRARY()

IF (HOST_OS_LINUX)
    DECLARE_EXTERNAL_RESOURCE(GOYNDEXER sbr:1454516000)
ELSEIF (HOST_OS_DARWIN)
    DECLARE_EXTERNAL_RESOURCE(GOYNDEXER sbr:1454515810)
ELSEIF (HOST_OS_WINDOWS)
    DECLARE_EXTERNAL_RESOURCE(GOYNDEXER sbr:1454515941)
ELSE()
    MESSAGE(FATAL_ERROR Unsupported host platform)
ENDIF()

END()
