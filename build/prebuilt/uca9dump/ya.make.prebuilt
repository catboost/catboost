IF (HOST_OS_DARWIN AND HOST_ARCH_X86_64 OR
    HOST_OS_LINUX AND HOST_ARCH_X86_64 OR
    HOST_OS_WINDOWS AND HOST_ARCH_X86_64)

    ENABLE(PREBUILT)

    PREBUILT_PROGRAM()

    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        ARCADIA_UCA9DUMP
        sbr:1623743666 FOR DARWIN
        sbr:1623751939 FOR LINUX
        sbr:1623746337 FOR WIN32
    )

    PRIMARY_OUTPUT(${ARCADIA_UCA9DUMP_RESOURCE_GLOBAL}/uca9dump${MODULE_SUFFIX})

    END()
ENDIF()
