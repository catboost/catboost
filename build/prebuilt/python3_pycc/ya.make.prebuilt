IF (HOST_OS_DARWIN AND HOST_ARCH_X86_64 OR
    HOST_OS_LINUX AND HOST_ARCH_X86_64 OR
    HOST_OS_WINDOWS AND HOST_ARCH_X86_64)

    ENABLE(PREBUILT)

    PREBUILT_PROGRAM()

    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        ARCADIA_PYTHON3_PYCC
        sbr:1643236195 FOR DARWIN
        sbr:1643236435 FOR LINUX
        sbr:1643236322 FOR WIN32
    )

    PRIMARY_OUTPUT(${ARCADIA_PYTHON3_PYCC_RESOURCE_GLOBAL}/pycc${MODULE_SUFFIX})

    END()
ENDIF()

