IF (HOST_OS_DARWIN AND HOST_ARCH_X86_64 OR
    HOST_OS_LINUX AND HOST_ARCH_X86_64 OR
    HOST_OS_WINDOWS AND HOST_ARCH_X86_64)

    ENABLE(PREBUILT)

    PREBUILT_PROGRAM()

    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        ARCADIA_FIX_ELF
        sbr:1623376503 FOR DARWIN
        sbr:1623378373 FOR LINUX
        sbr:1623377172 FOR WIN32
    )

    PRIMARY_OUTPUT(${ARCADIA_FIX_ELF_RESOURCE_GLOBAL}/fix_elf${MODULE_SUFFIX})

    END()
ENDIF()
