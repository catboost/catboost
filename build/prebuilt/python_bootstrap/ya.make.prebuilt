IF (HOST_OS_DARWIN AND HOST_ARCH_X86_64 OR
    HOST_OS_LINUX AND HOST_ARCH_X86_64 OR
    HOST_OS_WINDOWS AND HOST_ARCH_X86_64)

    ENABLE(PREBUILT)

    PREBUILT_PROGRAM()

    DECLARE_EXTERNAL_HOST_RESOURCES_BUNDLE(
        ARCADIA_PYTHON_BOOTSTRAP
        sbr:1623751539 FOR DARWIN
        sbr:1623751959 FOR LINUX
        sbr:1623751763 FOR WIN32
    )

    PRIMARY_OUTPUT(${ARCADIA_PYTHON_BOOTSTRAP_RESOURCE_GLOBAL}/bootstrap${MODULE_SUFFIX})

    END()
ENDIF()
