when ($CATBOOST_OPENSOURCE == "yes") {
    OPENSOURCE=yes
}

when ($OPENSOURCE) {
    LOCAL_YDB_DOCKER_PUBLIC_BUILD=yes
    USE_DYNAMIC_IDN=yes
    USE_DYNAMIC_AIO=yes
    USE_DYNAMIC_ICONV=yes
    USE_ASMLIB=no
}
