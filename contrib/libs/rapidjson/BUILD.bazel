# RapidJSON — header-only JSON parsing library.

cc_library(
    name = "rapidjson",
    hdrs = glob(["include/rapidjson/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
