{
  "name": "catboost",
  "version": "1.0.0",
  "description": "Node bindings for CatBoost library to apply models. CatBoost is a machine learning method based on gradient boosting over decision trees.",
  "main": "lib/index.js",
  "dependencies": {
    "node-addon-api": "^1.1.0"
  },
  "scripts": {
    "install": "./install.sh",
    "build": "./build.sh",
    "test": "npm run build && node ./lib/tests/model.js"
  },
  "author": "catboost.ai",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^7.0.22",
    "typescript": "^3.6.5"
  }
}
