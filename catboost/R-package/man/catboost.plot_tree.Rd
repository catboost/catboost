% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catboost.R
\name{catboost.plot_tree}
\alias{catboost.plot_tree}
\title{Export a CatBoost decision tree as Graphviz DOT}
\usage{
catboost.plot_tree(model, tree_idx, pool = NULL)
}
\arguments{
\item{model}{The model obtained as the result of training.}

\item{tree_idx}{Zero-based tree index.}

\item{pool}{Optional dataset. Required when the tree contains one-hot encoded categorical feature splits,
and useful for showing external feature indices from the dataset.}
}
\value{
A Graphviz DOT string with class \code{catboost.TreePlot}.
}
\description{
Returns a Graphviz DOT representation of a single tree from a CatBoost model.
Inner vertices correspond to splits and leaf vertices contain raw values predicted by the tree.
}
\examples{
\dontrun{
pool <- catboost.load_pool(features, target)
model <- catboost.train(pool, params = list(iterations = 2, depth = 2, loss_function = "RMSE"))
tree_dot <- catboost.plot_tree(model, tree_idx = 0, pool = pool)
cat(tree_dot)
writeLines(tree_dot, "tree.dot")
}
}
\seealso{
\url{https://catboost.ai/docs/concepts/python-reference_catboost_plot_tree.html}
}
