# getLossFunctionName

```java
public String getLossFunctionName()
```

#### {{ dl--purpose }}

Return the name of the loss function that the model has been trained with (for example, `Logloss` or `MultiClass`).

An empty string is returned if the model does not contain this information.
