# BUILD.bazel — root package
#
# The workspace root is on the include path via .bazelrc (--cxxopt=-I.).
# This makes #include <util/...> and #include <catboost/...> work from
# any package without repeating include flags.

package(default_visibility = ["//visibility:public"])
